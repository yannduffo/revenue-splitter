services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: splittr
      POSTGRES_PASSWORD: splittr
      POSTGRES_DB: splittr
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
